@using IMSWEB.Model
@model IMSWEB.CreateCashCollectionViewModel

@{
    ViewBag.Title = "Cash Collection Report";
}

<h4 class="inline-header">@(ViewBag.Title + ".")</h4>
<hr />
<div class="row">
    <div class="col-lg-4 col-md-4 col-sm-12 col-lg-offset-4 col-md-offset-4">
        <div class="panel panel-default">
            <div class="panel-body">
                @using (Ajax.BeginForm("CashCollectionReport", "Report", new { ReturnUrl = ViewBag.ReturnUrl }, ajaxOptions: new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "report", LoadingElementId = "spinningIcon" }, htmlAttributes: new { role = "form" }))
                {
                    <table class="table text-nowrap">
                        <tr>
                            <td>
                                <label>Customer Name</label>
                            </td>
                            <td>
                                @{Html.RenderAction("RenderPicker", "Picker", new { pickerType = PickerType.Customer, id = (Model != null && Model.CustomerID != null) ? Model.CustomerID : "0" });}
                                @Html.ValidationMessageFor(model => model.CustomerID, "", new { @class = "text-danger" })
                            </td>
                        </tr>
                        <tr>
                            <td>
                                @Html.Label("From Date", htmlAttributes: new { @class = "control-label" })
                            </td>
                            <td>
                                <div class='input-group date' id='FromDate'>
                                    <input type='text' class="input-sm form-control input-sm" name='FromDate' />
                                    <span class="input-group-addon">
                                        <i class="fa fa-calendar" aria-hidden="true"></i>
                                    </span>

                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                @Html.Label("To Date", htmlAttributes: new { @class = "control-label" })
                            </td>
                            <td>
                                <div class='input-group date' id='ToDate'>
                                    <input type='text' class="input-sm form-control input-sm" name='ToDate' />
                                    <span class="input-group-addon">
                                        <i class="fa fa-calendar" aria-hidden="true"></i>
                                    </span>
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <label for="sel1">Report Type</label>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control input-sm" name="ReportType" id="rdoAll">
                                        <option value="0" selected>All</option>
                                        <option value="1">Cash Sales</option>
                                        <option value="2">Cash Collection</option>
                                    </select>
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td></td>
                            <td>
                                <div class="col-sm-4">
                                    <img id="spinningIcon" height="45" width="45" style="display: none;" class="pull-right" src="~/Content/images/Spinner.gif" />
                                </div>
                                <div class="col-sm-4">
                                    <input id="btnPreview" type="submit" value="Preview" class="btn btn-sm btn-info" />
                                </div>
                            </td>
                        </tr>
                    </table>
                }
            </div>
        </div>
    </div>
</div>

<script>
    $('#FromDate').datetimepicker({
        defaultDate: moment(),
        format: 'DD-MMM-YYYY'
    });

    $('#ToDate').datetimepicker({
        defaultDate: moment(),
        format: 'DD-MMM-YYYY'
    });

    //$(function () {
    //    $(".rdoAll").prop("checked", "checked");
    //    prop
    //});

    //$('form').submit(function () {

    //    var name = $.trim($('#CustomersId').val());
    //    var chk = $('#rdoAll').prop("checked");

    //    if (name === '' && !chk) {
    //        toastr.info('No Customer Selected .');
    //        return false;
    //    }
    //    if (name != '') {
    //        $('#rdoAll').val = false;
    //    }

    //    if (chk) {
    //        $.trim($('#CustomersId').val()) = '';
    //    }

    //});
</script>
